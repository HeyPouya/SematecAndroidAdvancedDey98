<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sematec.android.advanced.dey98">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NullableVariableActivity"></activity>
        <activity android:name=".TestFunctionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TestLambdaActivity"></activity>
        <activity android:name=".TestRecyclerActivity" />
        <activity android:name=".TestJavaActivity" />
        <activity android:name=".testkotlin.MainActivity" />
    </application>

</manifest>